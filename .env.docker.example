# 本体平台容器部署用的变量。复制成 .env.docker 再改，**这个文件不要提交**（已在 .gitignore 里）。
#
#   cp .env.docker.example .env.docker
#   pnpm docker:up        # = docker compose --env-file .env.docker up -d --build
#
# 用 --env-file 之后，这个文件同时负责两件事：给容器注入变量，以及给 compose 做变量替换
# （所以下面第 0 节的 APP_PORT / VERSION_SNAPSHOT_DIR / NODE_IMAGE 也写这里）。

# ── 0. 编排参数（可选）────────────────────────────────────────────────────────
# 宿主机端口：本机 3000 被占用时换成 3100 之类。
APP_PORT=3000
# 装依赖用的 npm 源。国内网络直连 registry.npmjs.org 会让镜像构建里的 pnpm install 大面积超时
# （主机上的 ~/.npmrc 镜像源在镜像里读不到）。能直连官方源就删掉这行。
NPM_REGISTRY=https://registry.npmmirror.com
# 版本快照目录：**同一个平台库上的两套部署必须指向同一个目录**。本机开发服务写的是
# <项目>/.data/ontology-versions，容器指过去即可；留空则用命名卷（全新部署从空开始，重新发布一次）。
# Windows 路径直接写；Linux 服务器上写绝对路径，例如 /srv/ontology-versions。
# VERSION_SNAPSHOT_DIR=D:\project\ontology_management\.data\ontology-versions
# 拉不动 Docker Hub 时换镜像站。
# NODE_IMAGE=docker.1ms.run/node:24-bookworm-slim

# ── 0.1 本体存储（compose 里的 fuseki 服务）────────────────────────────────────
# 镜像：默认官方 stain/jena-fuseki:latest；国内可换成镜像站（同一份镜像）。
FUSEKI_IMAGE=stain/jena-fuseki:latest
# 数据集名：必须和平台里登记的 endpoint 路径一致（界面「存储资源」里的 …:3030/ds）。
FUSEKI_DATASET=ds
# Fuseki 管理密码。不填的话 Fuseki 会以无鉴权方式跑（compose 里因此是必填）。
FUSEKI_ADMIN_PASSWORD=change-me-fuseki-admin
# 宿主机端口：只给本机开发服务与浏览器管理界面用；平台容器走服务名 fuseki:3030。
FUSEKI_PORT=3030
# Fuseki 的数据目录（容器内 /fuseki = FUSEKI_BASE，数据集在 databases/ 下）。
# 默认落项目 .data/fuseki；也可以写成命名卷名字。
FUSEKI_DATA_DIR=./.data/fuseki

# ── 0.2 图库端点主机名改写（一般不用动）────────────────────────────────────────
# 平台库里登记的端点可能是给宿主机写的 http://localhost:3030/ds，而容器里 localhost 是容器自己。
# 默认值由 compose 给（localhost=fuseki）；要用外面的 Fuseki 时在这里覆盖，例如：
#   GRAPH_ENDPOINT_HOST_ALIAS=localhost=10.0.0.9:3030
# 多条用逗号分隔（只改主机/端口，协议与路径不动）。
# GRAPH_ENDPOINT_HOST_ALIAS=localhost=fuseki

# ── 1. 平台库（PostgreSQL，已在你自己的环境里跑着，不进 compose）────────────────
# 容器里不能用 localhost 指宿主机：localhost 是容器自己。用 host.docker.internal。
# 若 PostgreSQL 也在 Docker 里，让它发布端口（-p 5432:5432）后照样写 host.docker.internal；
# 注意容器里的用户名/密码要和实际授权一致，别照抄下面这行示例。
DATABASE_URL=postgresql://postgres:change-me@host.docker.internal:5432/ontology

# ── 2. 密钥 ────────────────────────────────────────────────────────────────────
# TARGET_ENCRYPTION_KEY：数据资源的凭据是用它加密后存进库的，**必须与库里已有数据用的那个一致**，
# 换一个就会解不开已登记的连接。生成：openssl rand -base64 32
TARGET_ENCRYPTION_KEY=replace-with-32-byte-base64-key
# AUTH_SECRET：签名会话 cookie 的随机串，至少 32 个字符。换掉只会让已登录会话失效。
AUTH_SECRET=replace-with-a-long-random-secret
# 会话 cookie 要不要带 Secure。留空 = 看这次请求实际是不是 https（反向代理后面看
# x-forwarded-proto），这是正常部署该用的行为：直接用 http 暴露时不会发 Secure cookie，
# 否则浏览器会丢掉它 —— 表现就是"登录了一下又回登录页"。
# 只有前端是 https、但反向代理没带 x-forwarded-proto 头时才需要显式写 true。
# AUTH_COOKIE_SECURE=

# ── 3. 首次初始化（只在平台库里还没有任何用户时生效）────────────────────────────
# 部署完调一次 POST /api/bootstrap 创建第一个管理员（库非空时返回 409，不会覆盖）。
BOOTSTRAP_ADMIN_EMAIL=admin@example.com
BOOTSTRAP_ADMIN_PASSWORD=change-me-now

# ── 4. 「能力验证」的推理模型（可选）────────────────────────────────────────────
# 不填时 /api/reasoning 那两页会明确报"模型未配置"，不会静默失败。
LLM_BASE_URL=
LLM_API_KEY=
LLM_MODEL=

# ── 5. 外部 MCP 客户端的访问令牌（可选）─────────────────────────────────────────
# 平台内「MCP 调试」页用会话 cookie，不需要它；给 Claude Code / Cursor 这类外部客户端才要。
MCP_API_TOKEN=

# ── 6. Oracle（一般不用改）────────────────────────────────────────────────────
# 镜像里已经装好 Linux 版 Instant Client，并把 ORACLE_CLIENT_LIB_DIR 指向 /opt/oracle/instantclient，
# 所以连旧服务端（11g 等，Thin 模式会被服务端拒掉）开箱可用，不用在这里配。
# 只有在你想**换成自备的客户端**时才需要：把 Linux 版解到 ./.data/oracle-client，打开
# docker-compose.yml 里被注释的那行挂载，再取消下面这行的注释。
# ORACLE_CLIENT_LIB_DIR=/opt/oracle/instantclient

# ── 7. 版本快照目录（要带旧数据时才需要看）──────────────────────────────────────
# 已发布版本的本体定义**不只在库里**：库里记"某本体发布了 v2"，定义本身在快照目录里。
# 换成新部署时如果不带过去，模型工具会答"本体还没有发布版本"、图库也对不上。
# 两个做法，选一个：
#   a) 把旧机器的 <项目>/.data/ontology-versions 整个拷过来，并用
#      VERSION_SNAPSHOT_DIR 指向它（这是给 compose 做变量替换的，写在 shell 环境里）：
#        PowerShell: $env:VERSION_SNAPSHOT_DIR="D:\project\ontology_management\.data\ontology-versions"
#        bash:       VERSION_SNAPSHOT_DIR=/srv/ontology-versions docker compose up -d
#   b) 什么都不带，部署完在界面「本体草稿」里重新发布一次，快照会写进命名卷。
