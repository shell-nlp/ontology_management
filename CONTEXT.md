# 本体管理

本上下文定义受管理图谱的本体，以及使其在 Neo4j 中生效的受控生命周期。

## 术语

**本体草稿**：
可编辑、可版本化的节点类型、关系类型及属性规则定义，尚未成为生效本体。
_避免使用_：工作 Schema、临时图谱

**已发布本体**：
指定为某一 Neo4j 目标生效定义的、已通过校验的本体版本。
_避免使用_：线上草稿、生产草稿

**发布**：
将一个已校验本体草稿受控提升为已发布本体的过程。
_避免使用_：保存、部署

**Neo4j 目标**：
一个具名 Neo4j 数据库连接，拥有与之绑定的本体草稿、已发布本体和查询会话。
_避免使用_：环境、服务器

**Cypher 会话**：
作用于一个 Neo4j 目标的交互式查询工作区，以只读或显式确认后的写入模式运行。
_避免使用_：终端、无限制控制台

**强制规则**：
本体属性上显式标记、发布时应转换为 Neo4j 约束或索引的校验与性能规则。
_避免使用_：默认约束、隐式规则

**业务实体**：
目标图谱中承载业务事实的节点和关系数据，不由本体发布自动创建。
_避免使用_：本体节点、样例数据

## 访问控制

**管理员**：
可管理 Neo4j 目标、本体草稿及发布，并可在确认后执行写入 Cypher 的平台角色。
_避免使用_：超级用户、操作员

**查看者**：
只能浏览本体和可视化信息，并执行只读 Cypher 查询的平台角色。
_避免使用_：访客、只读管理员

**本地账号**：
由平台自身管理、具有关联固定角色的登录身份，其密码仅以不可逆哈希形式保存。
_避免使用_：Neo4j 账号、匿名用户

**平台元数据**：
平台自身的账号、目标连接、本体草稿、发布记录和审计记录，独立于受管理图谱保存的持久化数据。
_避免使用_：业务图数据、Neo4j 配置节点

**平台 Schema**：
现有 PostgreSQL 数据库中名为 `ontology_platform` 的独立命名空间，承载全部平台元数据表。
_避免使用_：公共 Schema、业务 Schema

**目标凭据**：
用于连接一个 Neo4j 目标的认证信息，以密文保存在平台元数据中，并只能由服务端使用环境变量提供的主密钥解密。
_避免使用_：明文密码、浏览器密钥

## 本体与实例

**实体类型**：
本体中预先定义、发布后可被实体实例选用的类型，在 Neo4j 中映射为一个节点 Label。
_避免使用_：节点实例、关系标签

**关系类型**：
本体中预先定义、发布后可被关系实例选用的类型，在 Neo4j 中映射为一条边的 Relationship Type。
_避免使用_：关系标签、边实例

**实体实例**：
业务图谱中的一个节点，只能选择一个已发布实体类型作为其 Neo4j Label。
_避免使用_：实体类型、本体节点

**关系实例**：
连接两个实体实例的一条业务边，只能选择一个已发布关系类型作为其 Neo4j Relationship Type。
_避免使用_：关系类型、关系标签

**关系契约**：
关系类型声明的允许起始实体类型和终止实体类型；关系实例的两个端点必须严格符合该声明。
_避免使用_：自由连接、隐式方向

**属性定义**：
附属于一个实体类型或关系类型的属性规则，规定属性名称、数据类型、必填性以及唯一或索引要求。
_避免使用_：属性值、自由字段

**属性值**：
实体实例或关系实例上实际保存的字段值，必须符合其已发布类型的属性定义。
_避免使用_：属性定义、未校验字段

**首版属性类型**：
首版可用于属性定义的值类型：文本、整数、小数、布尔值、日期、日期时间、文本数组和 JSON。
_避免使用_：空间类型、时长、嵌套数组

**发布前校验**：
在本体草稿发布前对目标图谱既有数据执行的完整一致性检查；发现任何违反新规则的数据即阻止发布并给出报告。
_避免使用_：自动修复、带错发布

**本体视图**：
展示一个 Neo4j 目标全部已发布实体类型、关系类型和属性定义的管理视图，即使该类型尚无业务实例。
_避免使用_：运行时 Schema、实例图谱

**运行时 Schema 视图**：
由 `CALL db.schema.visualization()` 提供的发现视图，只展示目标 Neo4j 中已有实体或关系实例的类型。
_避免使用_：本体视图、配置预览
